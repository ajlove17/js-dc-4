var data = {

  todos: [

    'learn html',

    'learn css',

    'learn js'

  ]

}


/*var app = document.getElementById('app')
var templateSource = document.getElementById('to-do-template')
var template = Handlebars.compile( templateSource.innerHTML ) // gives me all the inner HTML form the other file. so i can get a rendered tempalte
var renderedTemplate = template( data )

app.innerHTML = renderedTemplate

console.log(renderedTemplate);*/


function renderedTemplate( date) {
  var app = document.
}


var form = document.getElementById('form');
form.addEventListener('submit', addToDo)

function addToDo ( e){
  e.preventDefault();
  var toDoinput = e.target.quer
}
